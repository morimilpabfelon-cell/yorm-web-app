name: Foundation 3A TypeScript diagnostic disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Foundation 3A TypeScript diagnostic completed"
